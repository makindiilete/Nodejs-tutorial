/*Now we will set our jwt Private key and our env variable

1) heroku config:set vidly_jwtPrivateKey=123999abc

Now we set our env variable to production
2) heroku config:set NODE_ENV=production

RUN "heroku config" to view all the env variables you have set.

NODE_ENV: production
vidly_jwtPrivateKey: 123999abc
*/
